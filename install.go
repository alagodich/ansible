package main

import (
	"log"
	"strings"
)

func main() {
	log.SetFlags(0)
	log.Println(strings.Repeat("=", 50))
	log.Println("Installing third party packages with go instead of ansible")

	// Nothing to install yet
}
